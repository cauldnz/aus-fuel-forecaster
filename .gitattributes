# Keep shell scripts and Linux-only config as LF, even when checked out on
# Windows. The devcontainer's bash interpreter rejects CRLF in shebang lines.
*.sh        text eol=lf
Dockerfile  text eol=lf
Makefile    text eol=lf
*.toml      text eol=lf
*.yaml      text eol=lf
*.yml       text eol=lf
